# Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional overrides
ANTHROPIC_MODEL=claude-sonnet-5
PORT=3001

# Optional: server-side interview storage (so interviews survive a browser
# refresh/reinstall instead of only living in localStorage). Get a free
# connection string from https://neon.tech — see README "Server-side
# interview storage" for step-by-step instructions. Leave unset to keep using
# localStorage only.
DATABASE_URL=
